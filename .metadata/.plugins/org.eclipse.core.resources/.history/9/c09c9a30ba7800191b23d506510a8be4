package org.xxjr.cust.annual;

import java.util.Date;

import org.ddq.common.constant.DuoduoConstant;
import org.ddq.common.context.AppParam;
import org.ddq.common.context.AppResult;
import org.ddq.common.core.service.BaseService;
import org.ddq.common.exception.AppException;
import org.ddq.common.exception.SysException;
import org.springframework.context.annotation.Lazy;
import org.springframework.stereotype.Service;
import org.springframework.util.StringUtils;
import org.ddq.common.exception.DuoduoError;

@Lazy
@Service
public class MeetingYaoSummaryService extends BaseService {
	private static final String NAMESPACE = "MEETINGYAOSUMMARY";

	/**
	 * querys
	 * @param params
	 * @return
	 */
	public AppResult query(AppParam params) {
		return super.query(params, NAMESPACE);
	}
	
	/**
	 * queryByPage
	 * @param params
	 * @return
	 */
	public AppResult queryByPage(AppParam params) {
		return super.queryByPage(params, NAMESPACE);
	}
	
	/**
	 * queryCount
	 * @param params
	 * @return
	 */
	public AppResult queryCount(AppParam params) {
		int size = getDao().count(NAMESPACE, super.COUNT,params.getAttr(),params.getDataBase());
		AppResult result = new AppResult();
		result.putAttr(DuoduoConstant.TOTAL_SIZE, size);
		return result;
	}
	
	/**
	 * 减奖项
	 * @param params
	 * @return
	 */
	public AppResult subtractLeftCount(AppParam params){
		AppResult result = new AppResult();
		int size = this.getDao().update(NAMESPACE, "subtractLeftCount", params.getAttr(), params.getDataBase());
		result.putAttr(DuoduoConstant.DAO_Update_SIZE, size);
		return result;
	}
	
	
	/**
	 * insert
	 * @param params
	 * @return
	 */
	public AppResult insert(AppParam params) {
		params.addAttr("createTime", new Date());
		return super.insert(params, NAMESPACE);
	}
	
	/**
	 * update
	 * @param params
	 * @return
	 */
	public AppResult update(AppParam params) {
		return super.update(params, NAMESPACE);
	}
	
	
	/**
	 * startDraw
	 * @param params
	 * @return
	 */
	public AppResult startDraw(AppParam params) {
		AppResult result = new AppResult();
		result = this.query(params);
		if(result.getRows().size() > 0){
			throw new SysException("活动已开始！");
		}
		params.addAttr("leftCount", params.getAttr("winNum"));
		return super.insert(params, NAMESPACE);
	}
	
	/**
	 * 当轮剩余奖品数量
	 * @param params
	 * @return
	 */
	public AppResult queryLeftCount(AppParam params){
		AppResult result = new AppResult();
		int size =0;
		try{
			size = getDao().count(NAMESPACE, "queryLeftCount",params.getAttr(),params.getDataBase());
		}catch(Exception e){
			
		}
		result.putAttr(DuoduoConstant.TOTAL_SIZE, size);
		return result;
	}
	
	/**
	 * delete
	 * @param params
	 * @return
	 */
	public AppResult delete(AppParam params) {
		String ids = (String) params.getAttr("ids");
		AppResult  result = null;
		if (!StringUtils.isEmpty(ids)) {
			for (String id : ids.split(",")) {
				AppParam param = new AppParam();
				param.addAttr("roundId", id);
				
				result = super.delete(param, NAMESPACE);
			}
		} else if (!StringUtils.isEmpty(params.getAttr("roundId"))) {
			result = super.delete(params, NAMESPACE);
		} else {
			throw new AppException(DuoduoError.DELETE_NO_ID);
		}
		return result;
	}
}
